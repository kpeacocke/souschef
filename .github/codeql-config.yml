paths:
  include:
    - souschef
    - tests
  exclude:
    - "**/test*"
    - "**/node_modules"

queries:
  - uses: security-extended
  - uses: security-and-quality

disable-default-queries: false

# Include custom models for path sanitizers
packs:
  - .github/codeql
